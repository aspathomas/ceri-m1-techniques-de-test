import org.junit.Test;
import static org.junit.Assert.assertEquals;

/**
 * Factory interface for class that aims to create IPokedex instance.
 *
 * @author fv
 */
public interface IPokedexFactoryTest {

	IPokedexTest createPokedex(IPokemonMetadataProviderTest metadataProvider, IPokemonFactoryTest pokemonFactory);

}
